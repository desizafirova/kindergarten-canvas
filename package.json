{
  "name": "kindergarten-canvas",
  "private": true,
  "version": "0.0.0",
  "scripts": {
    "dev": "cd frontend && bun install && bun run dev",
    "build": "cd frontend && bun install && bun run build",
    "preview": "cd frontend && bun run preview"
  }
}
